import { Request, Response, NextFunction } from 'express';
import { check, validationResult } from "express-validator";

export class EmployerDetailsValidator {

    constructor() {

    }
    // /**
    //  * Validator to check Input Parmaeter for employerDetails Api
    // */
   
}